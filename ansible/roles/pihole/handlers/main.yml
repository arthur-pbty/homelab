---
- name: Restart Pi-hole DNS
  ansible.builtin.command:
    cmd: docker exec pihole pihole restartdns
  changed_when: false

- name: Update Pi-hole Gravity
  ansible.builtin.command:
    cmd: docker exec pihole pihole -g
  register: gravity_update
  changed_when: true
